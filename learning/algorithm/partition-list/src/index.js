// 有个链表
import LinkedList from './linked_list'

const partition = (head,x) => {

}

//链表里有head属性 tail 

const linkedList = new LinkedList();
linkedList
  .append(1)
  .append(4)
  .append(3)
  .append(2)
  .append(5)
  .append(2)
  

partition(linkedList.head,3);
